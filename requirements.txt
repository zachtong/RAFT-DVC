# RAFT-DVC Dependencies
# This project was developed and tested on NVIDIA RTX 5090 (24GB VRAM)
# Recommended: CUDA-capable GPU with at least 16GB VRAM for training 128³ volumes

# Deep Learning Framework
# RTX 5090 (sm_120) requires PyTorch 2.4.0+ for proper CUDA support
torch==2.4.1
torchvision==0.19.1

# Scientific Computing
numpy>=1.21.0,<2.0.0
scipy>=1.7.0

# Data Processing
pyyaml>=5.4
h5py>=3.0.0
tifffile>=2021.0.0      # For loading experimental TIF images
imagecodecs>=2021.0.0   # TIF compression codecs support

# Visualization
matplotlib>=3.3.0
seaborn>=0.11.0

# Progress Tracking
tqdm>=4.60.0

# Logging and Monitoring
tensorboard>=2.8.0

# Optional: For memory profiling
# psutil>=5.8.0

# Optional: For high-quality 3D volume rendering (uncertainty visualization)
# pyvista>=0.38.0
# Note: The visualization module works without PyVista (uses matplotlib as fallback),
#       but PyVista provides significantly better rendering quality for 3D volumes.

# Installation Notes for RTX 5090:
# 1. Install NumPy first: pip install numpy==1.26.4
# 2. RTX 5090 requires PyTorch nightly with CUDA 12.8:
#    pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu128
# 3. For other GPUs (RTX 3090/4090 etc.), use stable PyTorch with CUDA 12.1:
#    pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu121
