# RAFT-DVC Dependencies
# Recommended: CUDA-capable GPU with at least 16GB VRAM for training 128³ volumes
#
# PyTorch: Install BEFORE running pip install -r requirements.txt
# The correct version depends on your GPU:
#   GH200 (TACC Vista):  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   RTX 5090 (sm_120):   pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu128
#   RTX 3090/4090:       pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Deep Learning Framework (install manually first, see above)
torch>=2.0.0
torchvision>=0.15.0

# Scientific Computing
numpy>=1.21.0,<2.0.0
scipy>=1.7.0

# Data Processing
pyyaml>=5.4
h5py>=3.0.0
tifffile>=2021.0.0      # For loading experimental TIF images
imagecodecs>=2021.0.0   # TIF compression codecs support

# Visualization
matplotlib>=3.3.0
seaborn>=0.11.0

# Progress Tracking
tqdm>=4.60.0

# Logging and Monitoring
tensorboard>=2.8.0

# Optional: For memory profiling
# psutil>=5.8.0

# Optional: For high-quality 3D volume rendering (uncertainty visualization)
# pyvista>=0.38.0
# Note: The visualization module works without PyVista (uses matplotlib as fallback),
#       but PyVista provides significantly better rendering quality for 3D volumes.

# Installation:
# 1. Install PyTorch first (see GPU-specific commands at top of this file)
# 2. Then: pip install -r requirements.txt
